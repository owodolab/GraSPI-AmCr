from skimage.measure import label 
from scipy import scipy.io
import numpy as np
    import os
def CC(namefile):
    
    pathfolder='/data-er/y.ameslon/StructureAndPerformance/GraSPI/GraSPI-AmCr/examples/5phaseMorphologies/HIERN-pipeline/Tempfiles/'
    exten='.mat'
    filemat= namefile + exten
    mat = scipy.io.loadmat(filemat)
    labeledImage = measure.label(twoLayersUp, connectivity=2) 
    savefile=pathfolder +filemat
    savemat(savefile, labeledImage)
    #return labeledImage
    